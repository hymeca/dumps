# PROCESS_HANDLE_TRACING_ENABLE

```C
typedef struct _PROCESS_HANDLE_TRACING_ENABLE { // same size in both x86 and x64
   ULONG Flags;                                                              // 0x000 0x000
} PROCESS_HANDLE_TRACING_ENABLE, *PPROCESS_HANDLE_TRACING_ENABLE;
```
