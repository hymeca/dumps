# SYSTEM_FLAGS_INFORMATION

```C
typedef struct _SYSTEM_FLAGS_INFORMATION { // same size in both x86 and x64
   ULONG Flags;                                                               // 0x000 0x000
} SYSTEM_FLAGS_INFORMATION, *PSYSTEM_FLAGS_INFORMATION;
```
