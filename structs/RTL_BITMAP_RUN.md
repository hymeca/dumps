# RTL_BITMAP_RUN

```C
typedef struct _RTL_BITMAP_RUN { // same size in both x86 and x64
   ULONG StartingIndex;                                  // 0x000 0x000
   ULONG NumberOfBits;                                   // 0x004 0x004
} RTL_BITMAP_RUN, *PRTL_BITMAP_RUN;
```
